feat(007): NIST Controls Knowledge Foundation - complete implementation

Implement all 32 tasks (T001-T032) for Feature 007: NIST SP 800-53 Rev 5
Controls Knowledge Foundation.

Core service:
- Rewrite NistControlsService with IMemoryCache, typed OSCAL deserialization,
  Polly resilience (exponential backoff, 3 retries), SemaphoreSlim concurrency
- Add 4 new INistControlsService methods: GetCatalogAsync, GetVersionAsync,
  GetControlEnhancementAsync, ValidateControlIdAsync
- Add typed OSCAL models (NistCatalogRoot, NistCatalog, ControlGroup, etc.)
- Split OSCAL types: Core owns OscalTypes.cs, Agents owns OscalModels.cs

Background services:
- NistControlsCacheWarmupService: BackgroundService with periodic refresh
  at 90% of cache TTL, 3-retry warmup with configurable initial delay
- ComplianceValidationService: validates 11 system-critical control IDs

Observability:
- NistControlsHealthCheck: IHealthCheck probing 3 sentinel controls
- ComplianceMetricsService: Counter (nist_api_calls_total) and
  Histogram (nist_api_call_duration_seconds) instruments
- ActivitySource instrumentation on GetCatalogAsync

MCP tools:
- NistControlSearchTool: search_nist_controls with query/family/max_results
- NistControlExplainerTool: explain_nist_control with statement/guidance/objectives
- Both registered in DI, ComplianceAgent, and ComplianceMcpTools

Configuration:
- NistControlsOptions with DataAnnotations validation (ValidateOnStart)
- Settings: BaseUrl, CacheDurationHours, TimeoutSeconds, MaxRetryAttempts,
  RetryDelaySeconds, EnableOfflineFallback, WarmupDelaySeconds

Tests (76 new):
- 35 NistControlsService unit tests (caching, fallback, typed models)
- 9 NistControlsCacheWarmupService tests (lifecycle, cancellation, retry)
- 7 NistControlsHealthCheck tests (healthy/degraded/unhealthy)
- 7 ComplianceValidationService tests (control mapping validation)
- 8 ComplianceMetricsService tests (counter/histogram verification)
- 13 NistControlToolTests (search + explainer with mocked service)
- 10 integration tests (full-stack roundtrip with embedded catalog)

Documentation:
- docs/nist-controls.md: architecture, config, API, tools, troubleshooting
- specs/007-nist-controls/: spec, plan, tasks (all 32 marked complete)
- XML doc comments on all new public types and members

Build: 0 errors, 0 warnings in modified files
Tests: 1,340 unit + 10 integration all passing
